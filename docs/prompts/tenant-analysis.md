# Tenant-Specific Analysis

We're seeing performance issues for tenant `{{TENANT_ID}}` in `{{SERVICE}}` (`{{ENV}}`).

Please:
1. Download recent profiles for the service
2. Filter analysis to samples tagged with this tenant
3. Identify what operations are expensive for this tenant
4. Compare against overall service behavior
5. Recommend tenant-specific or general optimizations
